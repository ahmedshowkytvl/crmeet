# إعداد GitHub Personal Access Token

دليل لإعداد GitHub Personal Access Token لاستخدامه مع سكريبتات النسخ الاحتياطي والاستعادة.

## 📋 لماذا نحتاج Token؟

GitHub توقف عن دعم المصادقة بكلمة المرور في أغسطس 2021. الآن يجب استخدام:
- **Personal Access Token (PAT)** للمصادقة عبر HTTPS
- **SSH Keys** للمصادقة عبر SSH

السكريبتات المحدثة تدعم استخدام Personal Access Token تلقائياً.

## 🔑 إنشاء Personal Access Token

### الخطوة 1: الذهاب إلى إعدادات GitHub

1. افتح GitHub وانتقل إلى [Settings](https://github.com/settings/profile)
2. في القائمة الجانبية، اضغط على **Developer settings**
3. اضغط على **Personal access tokens** > **Tokens (classic)**

### الخطوة 2: إنشاء Token جديد

1. اضغط على **Generate new token** > **Generate new token (classic)**
2. أدخل **Note** (اسم للتوكن): `CRM Backup Scripts`
3. اختر **Expiration** (مدة الصلاحية):
   - `No expiration` (للاستخدام الدائم)
   - أو اختر تاريخ انتهاء محدد
4. اختر الصلاحيات المطلوبة:
   - ✅ **repo** (Full control of private repositories)
     - هذا ضروري للقراءة والكتابة على المستودعات

### الخطوة 3: نسخ Token

1. اضغط على **Generate token**
2. **⚠️ مهم جداً**: انسخ Token فوراً - لن تتمكن من رؤيته مرة أخرى!
3. احفظ Token في مكان آمن

## ⚙️ إضافة Token إلى المشروع

### الطريقة 1: إضافة إلى ملف .env (موصى به)

أضف السطر التالي إلى ملف `.env`:

```env
GITHUB_TOKEN=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
```

**⚠️ تحذير أمني:**
- تأكد من أن `.env` موجود في `.gitignore`
- لا ترفع `.env` على Git
- لا تشارك Token مع أي شخص

### الطريقة 2: استخدام متغير البيئة

**Linux/Mac:**
```bash
export GITHUB_TOKEN=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
```

**Windows (Command Prompt):**
```cmd
set GITHUB_TOKEN=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
```

**Windows (PowerShell):**
```powershell
$env:GITHUB_TOKEN="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"
```

## ✅ التحقق من الإعداد

بعد إضافة Token، جرب تشغيل السكريبت:

```bash
# النسخ الاحتياطي
./backup_and_push.sh

# أو الاستعادة
./restore_from_git.sh
```

إذا كان Token صحيحاً، ستتم العملية بنجاح بدون طلب كلمة مرور.

## 🔒 الأمان

### نصائح الأمان:

1. **لا تشارك Token**:
   - لا ترفع Token على Git
   - لا تشاركه في الرسائل أو المحادثات
   - لا تضعه في الكود المصدري

2. **استخدم Token محدود الصلاحيات**:
   - اختر فقط الصلاحيات المطلوبة
   - استخدم Token منفصل لكل مشروع

3. **راجع Token بانتظام**:
   - احذف Token غير المستخدم
   - غيّر Token إذا تعرض للاختراق

4. **استخدم Token مع انتهاء صلاحية**:
   - تجنب Token بدون انتهاء صلاحية
   - حدد تاريخ انتهاء معقول

## 🐛 استكشاف الأخطاء

### خطأ: "Invalid username or token"

**الأسباب المحتملة:**
- Token غير صحيح
- Token منتهي الصلاحية
- Token لا يحتوي على الصلاحيات المطلوبة

**الحل:**
1. تحقق من Token في `.env`
2. تأكد من نسخ Token كاملاً بدون مسافات
3. أنشئ Token جديد إذا لزم الأمر

### خطأ: "Authentication failed"

**الأسباب المحتملة:**
- Token غير موجود في `.env`
- متغير البيئة غير مضبوط

**الحل:**
1. تأكد من وجود `GITHUB_TOKEN` في `.env`
2. تحقق من عدم وجود مسافات حول `=`
3. أعد تشغيل السكريبت

## 📝 ملاحظات

- السكريبتات تقرأ Token من `.env` تلقائياً
- إذا لم يكن Token موجوداً، ستستخدم المصادقة الحالية
- Token يُستخدم مؤقتاً في URL ثم يُزال تلقائياً
- URL الأصلي يُستعاد بعد كل عملية

## 🔄 تحديث Token

إذا احتجت تحديث Token:

1. أنشئ Token جديد من GitHub
2. حدّث `.env` بالـ Token الجديد
3. احذف Token القديم من GitHub

---

**آخر تحديث:** 2024-11-19

