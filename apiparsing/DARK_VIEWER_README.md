# 🌙 Dark Ticket Viewer

## نظرة عامة

Dark Ticket Viewer هو تطبيق ويب جديد بخلفية سوداء وواجهة احترافية **لا يعرض أبدًا** التذاكر المغلقة تلقائيًا (Auto Close).

## ✨ المميزات

### 🎨 التصميم
- **خلفية سوداء** أنيقة ومريحة للعين
- **ألوان خضراء** للتمييز والتركيز
- **تصميم متجاوب** يعمل على جميع الأجهزة
- **انتقالات سلسة** وتأثيرات بصرية

### 🚫 فلتر Auto Close
- **لا يعرض أبدًا** التذاكر التي تم إغلاقها بـ Auto Close
- **حماية مزدوجة**: فلترة في قائمة التذاكر + منع الوصول المباشر
- **إحصائيات واضحة** عن عدد التذاكر المستبعدة

### 📊 الإحصائيات المباشرة
- إجمالي التذاكر
- عدد التذاكر المستبعدة (Auto Close)
- التذاكر المتاحة
- التذاكر المفتوحة

## 🚀 كيفية التشغيل

### الطريقة الأولى: التشغيل المباشر
```bash
python ticket_web_viewer_dark.py
```

### الطريقة الثانية: استخدام ملف التشغيل
```bash
python launch_dark_viewer.py
```

## 🌐 الوصول للتطبيق

بعد التشغيل، افتح المتصفح واذهب إلى:
**http://localhost:5001**

## 📁 الملفات

### الملفات الأساسية
- `ticket_web_viewer_dark.py` - الخادم الرئيسي
- `templates/ticket_viewer_dark.html` - واجهة المستخدم السوداء
- `launch_dark_viewer.py` - ملف التشغيل السريع

### ملفات الاختبار
- `test_dark_viewer.py` - اختبار شامل للتطبيق
- `DARK_VIEWER_README.md` - هذا الملف

## 🔧 API Endpoints

### 1. الصفحة الرئيسية
```
GET http://localhost:5001/
```

### 2. جلب التذاكر (مع فلتر Auto Close)
```
GET http://localhost:5001/api/tickets
```

**الاستجابة:**
```json
{
  "success": true,
  "tickets": [...],
  "count": 20,
  "exclude_auto_close": true,
  "total_before_filter": 100,
  "auto_close_excluded": 5
}
```

### 3. إحصائيات التذاكر
```
GET http://localhost:5001/api/tickets/stats
```

**الاستجابة:**
```json
{
  "success": true,
  "stats": {
    "total_tickets": 100,
    "auto_close_count": 5,
    "manual_close_count": 44,
    "open_count": 51,
    "excluding_auto_close": 95,
    "dark_viewer_note": "Auto Close tickets are NEVER shown in this viewer"
  }
}
```

### 4. تفاصيل تذكرة محددة
```
GET http://localhost:5001/api/ticket/{ticket_id}
```

**ملاحظة:** إذا كانت التذكرة من نوع Auto Close، ستحصل على خطأ 403 Forbidden.

## 🛡️ الحماية من Auto Close

### 1. فلترة في قائمة التذاكر
```python
# في ticket_web_viewer_dark.py - السطر 232
tickets = [
    ticket for ticket in all_tickets
    if ticket.get('cf', {}).get('cf_closed_by') != 'Auto Close'
]
```

### 2. منع الوصول المباشر
```python
# في ticket_web_viewer_dark.py - السطر 285
cf_closed_by = ticket_response.get('cf', {}).get('cf_closed_by')
if cf_closed_by == 'Auto Close':
    return jsonify({'error': 'This ticket is not available (Auto Close)'}), 403
```

## 🎯 الفروق عن الـ Viewer العادي

| الميزة | Viewer العادي | Dark Viewer |
|--------|---------------|-------------|
| الخلفية | بيضاء | سوداء |
| Port | 5000 | 5001 |
| فلتر Auto Close | اختياري | إجباري |
| الوصول لـ Auto Close | ممكن | مستحيل |
| التصميم | عادي | احترافي |

## 🧪 الاختبار

### اختبار شامل
```bash
python test_dark_viewer.py
```

### اختبار API مباشرة
```bash
# اختبار الإحصائيات
curl http://localhost:5001/api/tickets/stats

# اختبار جلب التذاكر
curl http://localhost:5001/api/tickets

# اختبار تفاصيل تذكرة
curl http://localhost:5001/api/ticket/{ticket_id}
```

## 📱 الاستخدام

1. **تشغيل الخادم**: `python ticket_web_viewer_dark.py`
2. **فتح المتصفح**: http://localhost:5001
3. **تصفح التذاكر**: جميع التذاكر مرئية ماعدا Auto Close
4. **عرض التفاصيل**: انقر على أي تذكرة لعرض التفاصيل
5. **مراقبة الإحصائيات**: شريط الإحصائيات في الأعلى

## ⚠️ ملاحظات مهمة

1. **Auto Close محظور نهائيًا**: لا يمكن رؤية أو الوصول للتذاكر المغلقة تلقائيًا
2. **Port مختلف**: يستخدم port 5001 لتجنب التعارض مع الـ viewer العادي
3. **تصميم مخصص**: خلفية سوداء مع ألوان خضراء للتمييز
4. **أداء محسن**: فلترة سريعة وذاكرة تخزين مؤقت للأقسام والمستخدمين

## 🔄 التحديث التلقائي

التطبيق يحدث البيانات كل 30 ثانية تلقائيًا لضمان عرض أحدث المعلومات.

## 🆘 استكشاف الأخطاء

### خطأ 500
- تحقق من اتصال Zoho API
- تأكد من صحة الـ credentials في `config.py`

### خطأ 403
- هذا طبيعي للتذاكر المغلقة تلقائيًا
- التطبيق يمنع الوصول لها عمدًا

### خطأ الاتصال
- تأكد من تشغيل الخادم على port 5001
- تحقق من عدم وجود تطبيق آخر يستخدم نفس الـ port

---

**🌙 Dark Ticket Viewer - Professional, Secure, and Beautiful!**

