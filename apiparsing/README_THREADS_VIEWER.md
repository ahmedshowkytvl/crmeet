# عارض التذاكر والخيوط - Ticket Threads Viewer

تطبيق ويب لعرض تذاكر Zoho Desk مع إمكانية عرض الخيوط ومحتوى البريد الإلكتروني الكامل.

## المميزات

- 📋 عرض قائمة التذاكر مع التصفية التلقائية للتذاكر المغلقة تلقائياً
- 🔍 عرض تفاصيل التذكرة الكاملة
- 💬 عرض جميع الخيوط (البريد الإلكتروني، الملاحظات، الردود)
- 📧 عرض محتوى البريد الإلكتروني الكامل مع جميع التفاصيل
- 🔄 تحديث تلقائي للبيانات
- 📱 واجهة مستخدم عربية متجاوبة
- ⚡ واجهة برمجية (API) للوصول للبيانات

## النسخ المتاحة

### 1. النسخة التجريبية (Demo Version)
تطبيق يعمل ببيانات تجريبية بدون الحاجة لاتصال بـ API:

```bash
python launch_demo_threads_viewer.py
```

### 2. النسخة الكاملة (Full Version)
تطبيق يعمل مع Zoho Desk API الحقيقي:

```bash
python launch_threads_viewer.py
```

## المتطلبات

- Python 3.6 أو أحدث
- Flask
- Requests (للنسخة الكاملة فقط)
- اتصال بالإنترنت (للنسخة الكاملة فقط)
- بيانات اعتماد Zoho Desk API صحيحة (للنسخة الكاملة فقط)

## التثبيت

1. ثبت المتطلبات:
```bash
pip install flask requests
```

2. للنسخة الكاملة، تأكد من وجود ملف `config.py` مع بيانات الاعتماد الصحيحة

## التشغيل السريع

### النسخة التجريبية (موصى بها للاختبار)
```bash
python launch_demo_threads_viewer.py
```

### النسخة الكاملة
```bash
python launch_threads_viewer.py
```

ثم افتح المتصفح على: http://localhost:5000

## كيفية الاستخدام

### 1. عرض قائمة التذاكر
- افتح التطبيق في المتصفح
- ستظهر قائمة بالتذاكر المتاحة
- في النسخة التجريبية: 3 تذاكر تجريبية
- في النسخة الكاملة: آخر 50 تذكرة من النظام

### 2. عرض تفاصيل التذكرة
- اضغط على "عرض التفاصيل والخيوط" في أي تذكرة
- ستظهر معلومات التذكرة الكاملة
- ستظهر قائمة بجميع الخيوط

### 3. عرض محتوى الخيط الكامل
- في صفحة تفاصيل التذكرة، اضغط على "عرض المحتوى الكامل"
- ستظهر نافذة منبثقة بمحتوى البريد الإلكتروني الكامل
- أو اضغط على الخيط مباشرة للانتقال لصفحة منفصلة

## الواجهة البرمجية (API)

التطبيق يوفر واجهة برمجية للوصول للبيانات:

### جلب قائمة التذاكر
```
GET /api/tickets
```

### جلب تفاصيل التذكرة
```
GET /ticket/{ticket_id}
```

### جلب خيوط التذكرة
```
GET /api/ticket/{ticket_id}/threads
```

### جلب تفاصيل خيط معين
```
GET /api/thread/{ticket_id}/{thread_id}
```

## الملفات

### النسخة التجريبية
- `demo_threads_viewer.py` - التطبيق التجريبي الرئيسي
- `launch_demo_threads_viewer.py` - مشغل النسخة التجريبية

### النسخة الكاملة
- `ticket_threads_viewer.py` - التطبيق الرئيسي
- `launch_threads_viewer.py` - مشغل النسخة الكاملة
- `test_threads_viewer.py` - ملف اختبار API

### القوالب المشتركة
- `templates/` - قوالب HTML
  - `tickets_list.html` - قائمة التذاكر
  - `ticket_details.html` - تفاصيل التذكرة والخيوط
  - `thread_details.html` - محتوى الخيط الكامل
  - `error.html` - صفحة الأخطاء

### الملفات المساعدة
- `config.py` - إعدادات API (للنسخة الكاملة)
- `THREADS_VIEWER_README.md` - هذا الملف

## استكشاف الأخطاء

### النسخة التجريبية
- إذا لم يعمل التطبيق، تأكد من تثبيت Flask
- تأكد من عدم استخدام المنفذ 5000 من قبل تطبيق آخر

### النسخة الكاملة
### خطأ في الاتصال بـ API
- تأكد من صحة بيانات الاعتماد في `config.py`
- تأكد من اتصال الإنترنت
- تأكد من صحة Organization ID

### لا تظهر التذاكر
- تأكد من وجود تذاكر في النظام
- تأكد من صلاحيات API
- جرب تحديث الصفحة

### لا تظهر الخيوط
- بعض التذاكر قد لا تحتوي على خيوط
- تأكد من صلاحيات قراءة الخيوط

## الميزات المتقدمة

### التحديث التلقائي
- التطبيق يحدث البيانات كل 5 دقائق تلقائياً
- يمكن التحديث اليدوي باستخدام أزرار التحديث

### التصفية الذكية
- استبعاد تلقائي للتذاكر المغلقة تلقائياً
- عرض أولوية التذكرة بلون مميز
- عرض حالة التذكرة بوضوح

### الواجهة العربية
- دعم كامل للغة العربية
- اتجاه النص من اليمين لليسار
- أيقونات ورسوم توضيحية واضحة

## الاختبار

### اختبار النسخة التجريبية
```bash
python launch_demo_threads_viewer.py
```
ثم افتح http://localhost:5000

### اختبار النسخة الكاملة
```bash
python test_threads_viewer.py
```

## الدعم

إذا واجهت أي مشاكل:

### النسخة التجريبية
1. تأكد من تثبيت Flask
2. تأكد من عدم استخدام المنفذ 5000

### النسخة الكاملة
1. تأكد من صحة البيانات في `config.py`
2. تأكد من تثبيت جميع المتطلبات
3. تحقق من رسائل الخطأ في وحدة التحكم
4. جرب إعادة تشغيل التطبيق

## التطوير

للتطوير أو إضافة ميزات جديدة:
1. استخدم `demo_threads_viewer.py` كنقطة بداية للاختبار
2. استخدم `ticket_threads_viewer.py` للتطوير مع API الحقيقي
3. أضف المسارات الجديدة في Flask
4. أضف القوالب الجديدة في مجلد `templates/`
5. اختبر التطبيق قبل النشر

---

**ملاحظة:** تأكد من الحفاظ على أمان بيانات الاعتماد وعدم مشاركتها مع الآخرين.
