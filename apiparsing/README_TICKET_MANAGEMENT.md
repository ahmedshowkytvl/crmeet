# عارض التذاكر والخيوط - Ticket Threads Viewer

تطبيق ويب لعرض تذاكر Zoho Desk مع إمكانية عرض الخيوط ومحتوى البريد الإلكتروني الكامل **وإدارة التذاكر**.

## المميزات الجديدة ✨

### إدارة التذاكر
- 🔒 **إغلاق التذاكر** مع إمكانية إضافة تعليق
- 🔄 **إعادة فتح التذاكر** مع إمكانية إضافة تعليق
- ✏️ **تغيير حالة التذكرة** (مفتوح، معلق، مغلق، محلول)
- 💬 **إضافة تعليقات** للتذاكر
- 🔄 **تحديث تلقائي** للبيانات بعد كل إجراء

### الواجهة المحسنة
- أزرار تحكم واضحة في صفحة تفاصيل التذكرة
- نوافذ منبثقة لإدخال التعليقات وتغيير الحالة
- رسائل تأكيد قبل تنفيذ الإجراءات
- تحديث فوري للصفحة بعد كل إجراء

## النسخ المتاحة

### 1. النسخة التجريبية (Demo Version)
تطبيق يعمل ببيانات تجريبية بدون الحاجة لاتصال بـ API:

```bash
python launch_demo_threads_viewer.py
```

### 2. النسخة الكاملة (Full Version)
تطبيق يعمل مع Zoho Desk API الحقيقي:

```bash
python launch_threads_viewer.py
```

## المتطلبات

- Python 3.6 أو أحدث
- Flask
- Requests (للنسخة الكاملة فقط)
- اتصال بالإنترنت (للنسخة الكاملة فقط)
- بيانات اعتماد Zoho Desk API صحيحة (للنسخة الكاملة فقط)

## التثبيت

1. ثبت المتطلبات:
```bash
pip install flask requests
```

2. للنسخة الكاملة، تأكد من وجود ملف `config.py` مع بيانات الاعتماد الصحيحة

## التشغيل السريع

### النسخة التجريبية (موصى بها للاختبار)
```bash
python launch_demo_threads_viewer.py
```

### النسخة الكاملة
```bash
python launch_threads_viewer.py
```

ثم افتح المتصفح على: http://localhost:5000

## كيفية الاستخدام

### 1. عرض قائمة التذاكر
- افتح التطبيق في المتصفح
- ستظهر قائمة بالتذاكر المتاحة
- في النسخة التجريبية: 3 تذاكر تجريبية
- في النسخة الكاملة: آخر 50 تذكرة من النظام

### 2. عرض تفاصيل التذكرة
- اضغط على "عرض التفاصيل والخيوط" في أي تذكرة
- ستظهر معلومات التذكرة الكاملة
- ستظهر قائمة بجميع الخيوط
- **ستظهر أزرار التحكم في التذكرة**

### 3. إدارة التذكرة
#### إغلاق التذكرة
- اضغط على "إغلاق التذكرة" (إذا لم تكن مغلقة)
- أكد الإجراء
- أضف تعليقاً اختيارياً
- سيتم إغلاق التذكرة وتحديث الصفحة

#### إعادة فتح التذكرة
- اضغط على "إعادة فتح التذكرة" (إذا كانت مغلقة)
- أكد الإجراء
- أضف تعليقاً اختيارياً
- سيتم إعادة فتح التذكرة وتحديث الصفحة

#### تغيير حالة التذكرة
- اضغط على "تغيير الحالة"
- اختر الحالة الجديدة من القائمة
- أضف تعليقاً اختيارياً
- اضغط "تحديث الحالة"

#### إضافة تعليق
- اضغط على "إضافة تعليق"
- اكتب التعليق في النافذة المنبثقة
- اضغط "إضافة التعليق"
- سيتم إضافة التعليق كخيط جديد

### 4. عرض محتوى الخيط الكامل
- في صفحة تفاصيل التذكرة، اضغط على "عرض المحتوى الكامل"
- ستظهر نافذة منبثقة بمحتوى البريد الإلكتروني الكامل
- أو اضغط على الخيط مباشرة للانتقال لصفحة منفصلة

## الواجهة البرمجية (API)

التطبيق يوفر واجهة برمجية للوصول للبيانات وإدارة التذاكر:

### جلب البيانات
```
GET /api/tickets                    # قائمة التذاكر
GET /ticket/{ticket_id}             # تفاصيل التذكرة
GET /api/ticket/{ticket_id}/threads # خيوط التذكرة
GET /api/thread/{ticket_id}/{thread_id} # تفاصيل خيط معين
```

### إدارة التذاكر
```
POST /api/ticket/{ticket_id}/close      # إغلاق التذكرة
POST /api/ticket/{ticket_id}/reopen     # إعادة فتح التذكرة
POST /api/ticket/{ticket_id}/status     # تغيير حالة التذكرة
POST /api/ticket/{ticket_id}/comment    # إضافة تعليق
```

### أمثلة على استخدام API

#### إغلاق تذكرة
```bash
curl -X POST http://localhost:5000/api/ticket/123456789/close \
  -H "Content-Type: application/json" \
  -d '{"comment": "تم حل المشكلة"}'
```

#### تغيير حالة التذكرة
```bash
curl -X POST http://localhost:5000/api/ticket/123456789/status \
  -H "Content-Type: application/json" \
  -d '{"status": "Pending", "comment": "في انتظار رد العميل"}'
```

#### إضافة تعليق
```bash
curl -X POST http://localhost:5000/api/ticket/123456789/comment \
  -H "Content-Type: application/json" \
  -d '{"comment": "تم التواصل مع العميل"}'
```

## الملفات

### النسخة التجريبية
- `demo_threads_viewer.py` - التطبيق التجريبي الرئيسي
- `launch_demo_threads_viewer.py` - مشغل النسخة التجريبية

### النسخة الكاملة
- `ticket_threads_viewer.py` - التطبيق الرئيسي مع وظائف الإدارة
- `launch_threads_viewer.py` - مشغل النسخة الكاملة
- `test_threads_viewer.py` - ملف اختبار API الأساسي
- `test_ticket_management.py` - ملف اختبار وظائف الإدارة

### القوالب المشتركة
- `templates/` - قوالب HTML
  - `tickets_list.html` - قائمة التذاكر
  - `ticket_details.html` - تفاصيل التذكرة والخيوط مع أزرار التحكم
  - `thread_details.html` - محتوى الخيط الكامل
  - `error.html` - صفحة الأخطاء

### الملفات المساعدة
- `config.py` - إعدادات API (للنسخة الكاملة)
- `README_THREADS_VIEWER.md` - دليل الاستخدام الكامل

## الاختبار

### اختبار النسخة التجريبية
```bash
python launch_demo_threads_viewer.py
```
ثم افتح http://localhost:5000

### اختبار النسخة الكاملة
```bash
# اختبار API الأساسي
python test_threads_viewer.py

# اختبار وظائف الإدارة
python test_ticket_management.py
```

## استكشاف الأخطاء

### النسخة التجريبية
- إذا لم يعمل التطبيق، تأكد من تثبيت Flask
- تأكد من عدم استخدام المنفذ 5000 من قبل تطبيق آخر

### النسخة الكاملة
### خطأ في الاتصال بـ API
- تأكد من صحة بيانات الاعتماد في `config.py`
- تأكد من اتصال الإنترنت
- تأكد من صحة Organization ID

### خطأ في إدارة التذاكر
- تأكد من صلاحيات API (تحتاج صلاحيات UPDATE)
- تأكد من أن التذكرة موجودة
- تحقق من رسائل الخطأ في وحدة التحكم

### لا تظهر التذاكر
- تأكد من وجود تذاكر في النظام
- تأكد من صلاحيات API
- جرب تحديث الصفحة

### لا تظهر الخيوط
- بعض التذاكر قد لا تحتوي على خيوط
- تأكد من صلاحيات قراءة الخيوط

## الميزات المتقدمة

### التحديث التلقائي
- التطبيق يحدث البيانات كل 5 دقائق تلقائياً
- يمكن التحديث اليدوي باستخدام أزرار التحديث
- تحديث فوري بعد كل إجراء إدارة

### التصفية الذكية
- استبعاد تلقائي للتذاكر المغلقة تلقائياً
- عرض أولوية التذكرة بلون مميز
- عرض حالة التذكرة بوضوح

### الواجهة العربية
- دعم كامل للغة العربية
- اتجاه النص من اليمين لليسار
- أيقونات ورسوم توضيحية واضحة

### الأمان
- رسائل تأكيد قبل تنفيذ الإجراءات
- معالجة الأخطاء
- التحقق من صحة البيانات

## الدعم

إذا واجهت أي مشاكل:

### النسخة التجريبية
1. تأكد من تثبيت Flask
2. تأكد من عدم استخدام المنفذ 5000

### النسخة الكاملة
1. تأكد من صحة البيانات في `config.py`
2. تأكد من تثبيت جميع المتطلبات
3. تحقق من رسائل الخطأ في وحدة التحكم
4. جرب إعادة تشغيل التطبيق
5. تأكد من صلاحيات API (تحتاج UPDATE للتذاكر)

## التطوير

للتطوير أو إضافة ميزات جديدة:
1. استخدم `demo_threads_viewer.py` كنقطة بداية للاختبار
2. استخدم `ticket_threads_viewer.py` للتطوير مع API الحقيقي
3. أضف المسارات الجديدة في Flask
4. أضف القوالب الجديدة في مجلد `templates/`
5. اختبر التطبيق قبل النشر

---

**ملاحظة:** تأكد من الحفاظ على أمان بيانات الاعتماد وعدم مشاركتها مع الآخرين.
