# جلب التذاكر المغلقة يدوياً - Manual Closed Tickets Collector

تطبيق لجلب جميع التذاكر المغلقة يدوياً (غير Auto Close) خلال الشهر الحالي مع تفاصيلها الكاملة والخيوط.

## المميزات

- 📅 **جلب تلقائي للتذاكر المغلقة في الشهر الحالي**
- 🚫 **استبعاد تلقائي للتذاكر المغلقة تلقائياً (Auto Close)**
- 📊 **تحليل مفصل لمن أغلق التذاكر**
- 💬 **جلب جميع الخيوط لكل تذكرة**
- 📈 **تقرير إحصائي شامل**
- 💾 **حفظ البيانات بصيغة JSON**

## المتطلبات

- Python 3.6 أو أحدث
- Requests
- اتصال بالإنترنت
- بيانات اعتماد Zoho Desk API صحيحة

## التثبيت

1. تأكد من وجود ملف `config.py` مع بيانات الاعتماد الصحيحة
2. ثبت المتطلبات:
```bash
pip install requests
```

## التشغيل

### الطريقة السريعة
```bash
python launch_manual_closed_collector.py
```

### الطريقة اليدوية
```bash
python get_manual_closed_tickets.py
```

## كيفية العمل

### 1. جلب التذاكر المغلقة
- يبدأ التطبيق بجلب جميع التذاكر المغلقة من الشهر الحالي
- يستبعد تلقائياً التذاكر المغلقة تلقائياً (Auto Close)
- يجلب التذاكر على دفعات لتجنب حدود API

### 2. جمع التفاصيل الكاملة
- لكل تذكرة، يجلب التفاصيل الكاملة
- يجلب جميع الخيوط (البريد الإلكتروني، الملاحظات، الردود)
- يحسب عدد الخيوط لكل تذكرة

### 3. التحليل والإحصائيات
- يحلل من أغلق كل تذكرة
- يحلل التوزيع حسب الأقسام
- يحسب معدل النجاح
- يحسب متوسط الخيوط لكل تذكرة

### 4. حفظ البيانات
- يحفظ البيانات التفصيلية في ملف JSON
- يحفظ التقرير الإحصائي في ملف منفصل
- يستخدم timestamp في أسماء الملفات

## الملفات المُنشأة

### ملف البيانات التفصيلية
`manual_closed_tickets_YYYYMMDD_HHMMSS.json`
- يحتوي على جميع التذاكر مع تفاصيلها الكاملة
- يحتوي على جميع الخيوط لكل تذكرة
- يحتوي على معلومات وقت الجمع

### ملف التقرير الإحصائي
`manual_closed_summary_YYYYMMDD_HHMMSS.json`
- إحصائيات عامة
- توزيع من أغلق التذاكر
- توزيع حسب الأقسام
- معدلات النجاح والفشل

## مثال على البيانات المُجمعة

### تذكرة واحدة:
```json
{
  "ticket": {
    "id": "123456789",
    "ticketNumber": "TKT-001",
    "subject": "مشكلة في النظام",
    "status": "Closed",
    "closedTime": "2024-01-15T10:30:00Z",
    "cf": {
      "cf_closed_by": "أحمد محمد"
    },
    "department": {
      "name": "الدعم الفني"
    }
  },
  "threads": [
    {
      "id": "thread_001",
      "type": "Email",
      "content": "محتوى البريد الإلكتروني...",
      "fromAddress": "customer@example.com"
    }
  ],
  "threads_count": 1,
  "collected_at": "2024-01-15T12:00:00Z"
}
```

### التقرير الإحصائي:
```json
{
  "collection_date": "2024-01-15T12:00:00Z",
  "collection_period": "Current month (2024-01)",
  "total_tickets_collected": 150,
  "total_threads_collected": 450,
  "success_rate": "98.5%",
  "analysis": {
    "closed_by_distribution": {
      "أحمد محمد": 45,
      "فاطمة علي": 38,
      "محمد حسن": 32,
      "سارة أحمد": 25,
      "علي محمود": 10
    }
  }
}
```

## الاستخدام المتقدم

### تخصيص الحد الأقصى للتذاكر
يمكن تعديل الحد الأقصى في الكود:
```python
tickets_list = collector.get_manual_closed_tickets(limit=2000)  # بدلاً من 1000
```

### تغيير تأخير الطلبات
يمكن تعديل التأخير بين الطلبات:
```python
collected_data = collector.collect_tickets_details(ticket_ids, delay_between_requests=0.5)
```

### تخصيص فترة الجمع
يمكن تعديل فترة الجمع في الكود:
```python
# بدلاً من الشهر الحالي، يمكن تحديد فترة معينة
start_date = "2024-01-01"
end_date = "2024-01-31"
```

## استكشاف الأخطاء

### خطأ في الاتصال بـ API
- تأكد من صحة بيانات الاعتماد في `config.py`
- تأكد من اتصال الإنترنت
- تأكد من صحة Organization ID

### لا توجد تذاكر
- تأكد من وجود تذاكر مغلقة يدوياً في الشهر الحالي
- تأكد من صلاحيات API
- تحقق من رسائل الخطأ في وحدة التحكم

### بطء في الجمع
- زد التأخير بين الطلبات
- قلل حجم الدفعة
- تحقق من سرعة الإنترنت

## الميزات التقنية

### الأداء
- جمع على دفعات لتجنب حدود API
- تأخير قابل للتخصيص بين الطلبات
- تتبع التقدم في الوقت الفعلي

### الأمان
- معالجة الأخطاء
- إعادة المحاولة التلقائية
- حفظ التقدم

### المرونة
- قابل للتخصيص بسهولة
- دعم فترات زمنية مختلفة
- تصدير بيانات متعددة الصيغ

## الدعم

إذا واجهت أي مشاكل:
1. تأكد من صحة البيانات في `config.py`
2. تأكد من تثبيت جميع المتطلبات
3. تحقق من رسائل الخطأ في وحدة التحكم
4. جرب إعادة تشغيل التطبيق

## التطوير

للتطوير أو إضافة ميزات جديدة:
1. استخدم `get_manual_closed_tickets.py` كنقطة بداية
2. أضف فلاتر جديدة للبحث
3. أضف تحليلات إضافية
4. اختبر التطبيق قبل النشر

---

**ملاحظة:** تأكد من الحفاظ على أمان بيانات الاعتماد وعدم مشاركتها مع الآخرين.
