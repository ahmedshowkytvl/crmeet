# دليل الاستخدام - نظام مراقبة النظام

## 🎯 البدء السريع

### 1. تشغيل النظام
```bash
# Windows
start-system-monitor.bat

# Linux/Mac
./start-monitor-linux.sh

# PowerShell (Windows)
.\start-monitor.ps1
```

### 2. فتح المراقب
افتح المتصفح واذهب إلى:
```
http://localhost:8000/system-monitor
```

### 3. مشاركة الرابط
أرسل هذا الرابط لأي شخص على نفس الشبكة:
```
http://YOUR_IP:8000/system-monitor
```

## 📊 فهم الواجهة

### بطاقة الحالة العامة
- **🟢 أخضر**: النظام يعمل بشكل طبيعي
- **🟡 أصفر**: تحذيرات (استخدام عالي للموارد)
- **🔴 أحمر**: مشاكل خطيرة

### مقاييس النظام

#### معلومات الخادم
- **إصدار PHP**: الإصدار المستخدم
- **نظام التشغيل**: نوع النظام
- **حد الذاكرة**: الحد الأقصى المسموح
- **وقت الخادم**: الوقت الحالي

#### استخدام الذاكرة
- **النسبة المئوية**: نسبة الاستخدام
- **المستخدم**: مقدار الذاكرة المستخدمة
- **الذروة**: أعلى استخدام للذاكرة
- **المتاح**: مقدار الذاكرة المتاحة

#### استخدام القرص
- **النسبة المئوية**: نسبة الاستخدام
- **المستخدم**: مساحة القرص المستخدمة
- **المتاح**: المساحة المتاحة

#### قاعدة البيانات
- **الحالة**: متصل أم لا
- **النوع**: MySQL, MariaDB, etc.
- **الإصدار**: إصدار قاعدة البيانات
- **الاتصالات**: الحالية/الحد الأقصى

#### وقت الاستجابة
- **الوقت**: سرعة التطبيق بالميلي ثانية
- **المتوسط**: متوسط وقت الاستجابة

#### المستخدمون النشطون
- **العدد**: عدد المستخدمين المتصلين
- **القائمة**: أسماء المستخدمين النشطين
- **آخر نشاط**: وقت آخر نشاط لكل مستخدم

### الرسوم البيانية

#### رسم استخدام الذاكرة
- **المحور X**: الوقت
- **المحور Y**: نسبة الاستخدام (0-100%)
- **اللون**: أزرق
- **التحديث**: كل 5 ثوان

#### رسم وقت الاستجابة
- **المحور X**: الوقت
- **المحور Y**: وقت الاستجابة بالميلي ثانية
- **اللون**: وردي
- **التحديث**: كل 5 ثوان

## 🔔 التنبيهات

### أنواع التنبيهات
- **تحذير**: استخدام عالي للموارد
- **خطأ**: مشاكل في النظام
- **معلومات**: تحديثات النظام

### متى تظهر التنبيهات
- **استخدام الذاكرة > 90%**: تحذير
- **استخدام القرص > 90%**: تحذير
- **وقت الاستجابة > 2 ثانية**: تحذير
- **فشل قاعدة البيانات**: خطأ
- **بدء النظام**: معلومات

### إدارة التنبيهات
- **إغلاق**: اضغط X لإغلاق التنبيه
- **تلقائي**: تختفي بعد 5 ثوان
- **متعددة**: يمكن عرض عدة تنبيهات

## ⚙️ الإعدادات

### تغيير فترات التحديث
```javascript
// في ملف JavaScript
this.refreshInterval = 5000; // 5 ثوان
```

### إضافة مقاييس جديدة
```php
// في SystemMonitorController.php
private function getCustomMetrics() {
    return [
        'custom_metric' => 'value'
    ];
}
```

### تخصيص التنبيهات
```javascript
// في websocket-server.js
if (memoryUsage > 500) {
    alerts.push({
        type: 'warning',
        title: 'استخدام الذاكرة عالي',
        message: `استخدام الذاكرة: ${memoryUsage} MB`
    });
}
```

## 🌐 الوصول من الشبكة

### معرفة عنوان IP

#### Windows
```cmd
ipconfig
```

#### Linux/Mac
```bash
ifconfig
# أو
ip addr show
```

### مثال
إذا كان عنوان IP هو `192.168.1.100`:
```
http://192.168.1.100:8000/system-monitor
```

### إعدادات الجدار الناري

#### Windows
1. افتح Windows Defender Firewall
2. اضغط "Advanced settings"
3. اضغط "Inbound Rules" → "New Rule"
4. اختر "Port" → "TCP" → "8000,8080"
5. اختر "Allow the connection"

#### Linux
```bash
sudo ufw allow 8000
sudo ufw allow 8080
```

## 🔧 استكشاف الأخطاء

### مشاكل شائعة

#### 1. الصفحة لا تظهر
- تحقق من أن Laravel يعمل
- تأكد من المنفذ 8000
- تحقق من السجلات

#### 2. لا توجد تحديثات فورية
- تحقق من WebSocket
- تأكد من المنفذ 8080
- تحقق من console المتصفح

#### 3. مشاكل في البيانات
- تحقق من قاعدة البيانات
- تأكد من الاتصال
- تحقق من الصلاحيات

#### 4. مشاكل في الشبكة
- تحقق من الجدار الناري
- تأكد من عنوان IP
- تحقق من الاتصال

### سجلات النظام

#### سجلات Laravel
```bash
tail -f storage/logs/laravel.log
```

#### سجلات WebSocket
تظهر في console الخادم

#### سجلات المتصفح
اضغط F12 → Console

## 📱 الاستخدام على الأجهزة المحمولة

### المتصفحات المدعومة
- **Chrome**: مدعوم بالكامل
- **Firefox**: مدعوم بالكامل
- **Safari**: مدعوم بالكامل
- **Edge**: مدعوم بالكامل

### التصميم المتجاوب
- **الهواتف**: واجهة محسنة للهواتف
- **الأجهزة اللوحية**: تخطيط مناسب للشاشات المتوسطة
- **أجهزة الكمبيوتر**: واجهة كاملة الميزات

### النصائح
- استخدم الاتصال الأفقي للهواتف
- تأكد من اتصال WiFi قوي
- أغلق التطبيقات غير الضرورية

## 🎨 التخصيص

### تغيير الألوان
```css
/* في ملف CSS */
.status-card.success {
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}
```

### إضافة مقاييس جديدة
```php
// في SystemMonitorController.php
private function getCustomMetrics() {
    return [
        'custom_metric' => 'value'
    ];
}
```

### تخصيص التنبيهات
```javascript
// في websocket-server.js
if (customCondition) {
    alerts.push({
        type: 'info',
        title: 'تنبيه مخصص',
        message: 'رسالة مخصصة'
    });
}
```

## 📊 مراقبة الأداء

### مقاييس مهمة
- **استخدام الذاكرة**: يجب أن يكون أقل من 90%
- **استخدام القرص**: يجب أن يكون أقل من 90%
- **وقت الاستجابة**: يجب أن يكون أقل من 2 ثانية
- **اتصالات قاعدة البيانات**: يجب أن تكون أقل من 80%

### تحسين الأداء
- استخدم Redis للكاش
- فعّل OPcache
- قلل فترات التحديث
- استخدم CDN للثوابت

## 🛡️ الأمان

### إعدادات الأمان الأساسية
- **تقييد IP**: السماح فقط للشبكة المحلية
- **المصادقة**: تفعيل تسجيل الدخول
- **API Key**: استخدام مفتاح API

### إعدادات الأمان المتقدمة
- **HTTPS**: استخدام اتصال آمن
- **تشفير**: تشفير البيانات الحساسة
- **مراجعة**: مراجعة السجلات بانتظام

## 📞 الدعم

### الحصول على المساعدة
1. راجع هذا الدليل
2. تحقق من `SYSTEM_MONITOR_README.md`
3. راجع سجلات النظام
4. تأكد من المتطلبات

### التطوير
- أضف ميزات جديدة
- حسّن الواجهة
- أضف تنبيهات جديدة
- حسّن الأداء

---

**نصيحة**: احفظ هذا الدليل للرجوع إليه لاحقاً! 📚






