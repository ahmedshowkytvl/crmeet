# 🔍 محلل الويب الشامل - دليل الاستخدام

## نظرة عامة

هذا النظام يوفر تحليل شامل لتطبيقات الويب باستخدام Playwright، ويتضمن:

- 🕷️ **زحف ذكي** للصفحات مع تجنب الحلقات اللانهائية
- 📊 **تحليل الأداء** مع قياس TTFB ووقت التحميل
- ♿ **فحص إمكانية الوصول** باستخدام axe-core
- 🔍 **اكتشاف المشاكل** مع تصنيف حسب الخطورة
- 📋 **تقارير تفصيلية** بصيغ HTML, JSON, CSV
- 🖼️ **لقطات شاشة** كاملة للصفحات
- 📄 **نسخ HTML** للتحليل اللاحق

## التثبيت والإعداد

### 1. التأكد من تثبيت المتطلبات

```bash
# التأكد من تثبيت Node.js
node --version

# التأكد من تثبيت Playwright
npm list playwright
```

### 2. تثبيت Playwright (إذا لم يكن مثبتاً)

```bash
npm install playwright
npx playwright install chromium
```

### 3. إعداد ملف التكوين

```bash
# نسخ ملف التكوين الافتراضي
cp crawler_config.json my_config.json

# تعديل الإعدادات حسب الحاجة
```

## الاستخدام

### الطريقة الأساسية

```bash
# تشغيل التحليل بالإعدادات الافتراضية
node run_crawler.js
```

### مع خيارات مخصصة

```bash
# تحديد رابط مختلف
node run_crawler.js --url http://localhost:8000

# زيادة عمق الزحف
node run_crawler.js --depth 3

# تغيير مجلد الإخراج
node run_crawler.js --output ./my_results

# تشغيل مع واجهة مرئية (للتطوير)
node run_crawler.js --headless false

# عرض تفاصيل أكثر
node run_crawler.js --verbose
```

### استخدام ملف تكوين مخصص

```bash
node run_crawler.js --config my_config.json
```

## ملف التكوين

### الإعدادات الأساسية

```json
{
  "START_URL": "http://192.168.15.29:8000",
  "MAX_DEPTH": 3,
  "ALLOWED_DOMAINS": ["192.168.15.29:8000"],
  "RATE_LIMIT_MS": 500,
  "OUTPUT_DIR": "./mcp_output",
  "VISIT_TIMEOUT_MS": 30000,
  "VIEWPORT": { "width": 1366, "height": 768 },
  "INCLUDE_A11Y": true,
  "HEADLESS": true
}
```

### إعدادات متقدمة

```json
{
  "AUTH": {
    "type": "cookie",
    "value": [
      {
        "name": "session_id",
        "value": "abc123",
        "domain": "192.168.15.29:8000",
        "path": "/"
      }
    ]
  },
  "PERFORMANCE_THRESHOLDS": {
    "TTFB_MS": 1500,
    "DOM_LOAD_MS": 3000,
    "FULL_LOAD_MS": 5000
  },
  "ACCESSIBILITY_RULES": {
    "color-contrast": true,
    "keyboard-navigation": true,
    "aria-labels": true
  }
}
```

## أنواع المصادقة المدعومة

### 1. المصادقة بـ Cookie

```json
{
  "AUTH": {
    "type": "cookie",
    "value": [
      {
        "name": "session_token",
        "value": "your_session_value",
        "domain": "example.com",
        "path": "/"
      }
    ]
  }
}
```

### 2. المصادقة الأساسية

```json
{
  "AUTH": {
    "type": "basic",
    "value": {
      "username": "your_username",
      "password": "your_password"
    }
  }
}
```

## ملفات الإخراج

### 1. التقارير

- **`reports/report.html`** - تقرير HTML شامل مع واجهة تفاعلية
- **`reports/report.json`** - بيانات JSON كاملة للتحليل البرمجي
- **`reports/report.csv`** - بيانات CSV للتحليل في Excel أو أدوات أخرى

### 2. الملفات المرئية

- **`screenshots/`** - لقطات شاشة كاملة لكل صفحة
- **`html/`** - نسخ HTML من كل صفحة

### 3. السجلات

- **`raw_logs/`** - سجلات مفصلة لكل صفحة

## تصنيف المشاكل

### 🚨 حرجة (Critical)
- أخطاء خادم 5xx
- استثناءات JavaScript تعطل الواجهة
- فشل بوابة المصادقة
- تدفقات الدفع المعطلة

### ⚠️ عالية (High)
- أخطاء 4xx للموارد المهمة
- أخطاء كثيرة في وحدة التحكم
- فشل إمكانية الوصول للعناصر الحرجة
- صور مكسورة في الصفحة الرئيسية

### 📊 متوسطة (Medium)
- TTFB بطيء (>1.5s)
- رؤوس cache مفقودة للملفات الكبيرة
- مشاكل أداء متوسطة

### 📝 منخفضة (Low)
- صور زخرفية بدون alt
- اقتراحات تحسين أداء طفيفة

## أمثلة عملية

### تحليل موقع محلي

```bash
# تحليل موقع Laravel محلي
node run_crawler.js --url http://localhost:8000 --depth 2
```

### تحليل مع مصادقة

```json
{
  "START_URL": "http://192.168.15.29:8000/login",
  "AUTH": {
    "type": "form",
    "value": {
      "username": "admin",
      "password": "password"
    }
  }
}
```

### تحليل شامل مع إعدادات مخصصة

```bash
node run_crawler.js \
  --url http://192.168.15.29:8000 \
  --depth 4 \
  --output ./comprehensive_analysis \
  --verbose
```

## استكشاف الأخطاء

### مشاكل شائعة

1. **خطأ في تثبيت Playwright**
   ```bash
   npx playwright install chromium
   ```

2. **مهلة زمنية منتهية**
   - زيادة `VISIT_TIMEOUT_MS` في التكوين
   - تقليل `MAX_DEPTH`

3. **مشاكل الذاكرة**
   - تقليل `RATE_LIMIT_MS`
   - استخدام `HEADLESS: true`

4. **فشل المصادقة**
   - التحقق من صحة بيانات المصادقة
   - التأكد من أن الجلسة لم تنته

### وضع التطوير

```bash
# تشغيل مع واجهة مرئية للمراقبة
node run_crawler.js --headless false --verbose
```

## التكامل مع أنظمة أخرى

### تحليل البرامج النصية

```javascript
import WebCrawlerAnalyzer from './web_crawler_analyzer.js';

const analyzer = new WebCrawlerAnalyzer({
  START_URL: "http://example.com",
  MAX_DEPTH: 2
});

await analyzer.init();
await analyzer.crawl();
const results = analyzer.results;
```

### تحليل النتائج برمجياً

```javascript
// قراءة نتائج JSON
const results = JSON.parse(fs.readFileSync('./mcp_output/reports/report.json'));

// تصفية المشاكل الحرجة
const criticalIssues = results.pages.flatMap(page => 
  page.issues.filter(issue => issue.severity === 'critical')
);
```

## نصائح للأداء الأمثل

1. **استخدم HEADLESS: true** للإنتاج
2. **اضبط RATE_LIMIT_MS** حسب قدرة الخادم
3. **قلل MAX_DEPTH** للمواقع الكبيرة
4. **استخدم ALLOWED_DOMAINS** لتجنب الزحف غير المرغوب فيه
5. **فعل INCLUDE_A11Y** فقط عند الحاجة

## الدعم والمساعدة

```bash
# عرض المساعدة
node run_crawler.js --help
```

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام التجاري والشخصي.










