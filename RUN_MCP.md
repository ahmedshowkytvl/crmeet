# 🚀 تشغيل MCP

## طرق تشغيل MCP

### 1. تشغيل مباشر (الأسرع)
```bash
MYSQL_HOST=127.0.0.1 MYSQL_PORT=3306 MYSQL_USER=root MYSQL_PASSWORD="" MYSQL_DATABASE=crm npx -y @benborla29/mcp-server-mysql
```

### 2. تشغيل باستخدام سكريبت Node.js
```bash
node start-mcp-server.js
```

### 3. تشغيل باستخدام ملف Batch (Windows)
```cmd
start-mcp.bat
```

### 4. تشغيل باستخدام PowerShell (Windows)
```powershell
.\start-mcp.ps1
```

## 📋 إعدادات قاعدة البيانات

- **Host:** 127.0.0.1
- **Port:** 3306
- **Database:** crm
- **User:** root
- **Password:** (فارغ)

## ✅ التحقق من عمل MCP

بعد تشغيل MCP، ستظهر رسائل مثل:
```
🚀 بدء تشغيل خادم MCP مع MySQL...
📋 إعدادات الاتصال:
Host: 127.0.0.1
Port: 3306
Database: crm
User: root
✅ خادم MCP يعمل الآن!
```

## 🔧 إضافة MCP إلى Cursor

1. افتح Cursor
2. اذهب إلى Settings > Extensions > MCP
3. أضف ملف `mcp-final-config.json`

## 🛑 إيقاف MCP

اضغط `Ctrl+C` في نافذة الأوامر لإيقاف خادم MCP.

## 🆘 استكشاف الأخطاء

### إذا لم يعمل MCP:
1. تأكد من تشغيل MySQL
2. تحقق من إعدادات الاتصال
3. جرب إعادة تشغيل MCP

### إذا لم تظهر رسائل:
- انتظر بضع ثوان
- تحقق من نافذة الأوامر
- جرب طريقة تشغيل أخرى
