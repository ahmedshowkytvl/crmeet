# نظام مراقبة النظام في الوقت الفعلي

## نظرة عامة

نظام مراقبة شامل يسمح لأي شخص على نفس الشبكة بمراقبة حالة النظام والأداء في الوقت الفعلي. يتضمن النظام:

- **مراقبة الخادم**: معلومات PHP، الذاكرة، المعالج، القرص
- **مراقبة قاعدة البيانات**: حالة الاتصال، الإحصائيات، الأداء
- **مراقبة التطبيق**: إحصائيات Laravel، المستخدمين النشطين
- **رسوم بيانية تفاعلية**: عرض البيانات في الوقت الفعلي
- **تنبيهات فورية**: إشعارات عند حدوث مشاكل
- **اتصال WebSocket**: تحديثات فورية بدون إعادة تحميل

## الميزات

### 🔍 مراقبة شاملة
- **معلومات الخادم**: إصدار PHP، نظام التشغيل، حدود الذاكرة
- **استخدام الموارد**: الذاكرة، المعالج، مساحة القرص
- **قاعدة البيانات**: حالة الاتصال، عدد الاتصالات، أداء الاستعلامات
- **المستخدمين النشطين**: قائمة المستخدمين المتصلين حالياً

### 📊 رسوم بيانية تفاعلية
- **رسم استخدام الذاكرة**: تتبع استخدام الذاكرة مع الوقت
- **رسم وقت الاستجابة**: مراقبة أداء التطبيق
- **مؤشرات الأداء**: عرض مقاييس الأداء المختلفة

### ⚡ تحديثات فورية
- **WebSocket**: اتصال مباشر للتحديثات الفورية
- **Polling احتياطي**: نظام احتياطي في حالة فشل WebSocket
- **تنبيهات ذكية**: إشعارات عند تجاوز الحدود المسموحة

### 🎨 واجهة مستخدم متقدمة
- **تصميم متجاوب**: يعمل على جميع الأجهزة
- **واجهة عربية**: دعم كامل للغة العربية
- **ألوان ديناميكية**: تغيير الألوان حسب حالة النظام
- **تحديث تلقائي**: تحديث البيانات كل 5 ثوان

## التثبيت والتشغيل

### المتطلبات
- PHP 8.2+
- Laravel 12
- Node.js 16+
- MySQL/MariaDB

### الخطوة 1: تثبيت التبعيات

```bash
# تبعيات PHP (موجودة بالفعل)
composer install

# تبعيات WebSocket
npm install ws
```

### الخطوة 2: تشغيل النظام

#### على Windows:
```cmd
start-system-monitor.bat
```

#### على Linux/Mac:
```bash
./start-system-monitor.sh
```

#### تشغيل يدوي:
```bash
# Terminal 1: خادم Laravel
php artisan serve --host=0.0.0.0 --port=8000

# Terminal 2: خادم WebSocket
node websocket-server.js
```

### الخطوة 3: الوصول للنظام

افتح المتصفح واذهب إلى:
```
http://localhost:8000/system-monitor
```

## الاستخدام

### الوصول للمراقب
1. افتح المتصفح
2. اذهب إلى `http://YOUR_SERVER_IP:8000/system-monitor`
3. ستظهر واجهة المراقب مع البيانات الحية

### فهم الواجهة

#### بطاقة الحالة العامة
- **أخضر**: النظام يعمل بشكل طبيعي
- **أصفر**: تحذيرات (استخدام عالي للموارد)
- **أحمر**: مشاكل خطيرة

#### مقاييس النظام
- **استخدام الذاكرة**: النسبة المئوية والتفاصيل
- **استخدام القرص**: مساحة القرص المستخدمة
- **وقت الاستجابة**: سرعة التطبيق
- **المستخدمين النشطين**: عدد المستخدمين المتصلين

#### الرسوم البيانية
- **رسم الذاكرة**: تتبع استخدام الذاكرة مع الوقت
- **رسم الاستجابة**: مراقبة أداء التطبيق

### التنبيهات
النظام يرسل تنبيهات تلقائية عند:
- تجاوز استخدام الذاكرة 90%
- تجاوز استخدام القرص 90%
- بطء في الاستجابة
- مشاكل في قاعدة البيانات

## التخصيص

### تغيير فترات التحديث
```javascript
// في system-monitor-websocket.js
this.refreshInterval = 5000; // 5 ثوان
this.heartbeatInterval = 30000; // 30 ثانية
```

### إضافة مقاييس جديدة
```php
// في SystemMonitorController.php
private function getCustomMetrics() {
    return [
        'custom_metric' => 'value'
    ];
}
```

### تخصيص التنبيهات
```javascript
// في websocket-server.js
if (memoryUsage > 500) { // تغيير الحد
    alerts.push({
        type: 'warning',
        title: 'استخدام الذاكرة عالي',
        message: `استخدام الذاكرة: ${memoryUsage} MB`
    });
}
```

## الأمان

### حماية الوصول
```php
// في routes/web.php
Route::middleware(['auth'])->group(function () {
    Route::get('/system-monitor', [SystemMonitorController::class, 'index']);
});
```

### تقييد IP
```php
// في SystemMonitorController.php
public function index() {
    $allowedIPs = ['192.168.1.0/24']; // الشبكة المحلية فقط
    if (!$this->isIPAllowed(request()->ip(), $allowedIPs)) {
        abort(403, 'غير مسموح بالوصول');
    }
    return view('system-monitor.index');
}
```

## استكشاف الأخطاء

### مشاكل شائعة

#### WebSocket لا يعمل
```bash
# تحقق من المنفذ
netstat -an | grep 8080

# تحقق من Node.js
node --version
```

#### Laravel لا يعمل
```bash
# تحقق من المنفذ
netstat -an | grep 8000

# تحقق من PHP
php --version
```

#### مشاكل قاعدة البيانات
```bash
# تحقق من الاتصال
php artisan tinker
>>> DB::connection()->getPdo();
```

### سجلات النظام
```bash
# سجلات Laravel
tail -f storage/logs/laravel.log

# سجلات WebSocket
# تظهر في console الخادم
```

## الأداء

### تحسين الأداء
- استخدم Redis للكاش
- فعّل OPcache
- استخدم CDN للثوابت
- قلل فترات التحديث

### مراقبة الأداء
- راقب استخدام الذاكرة
- تابع وقت الاستجابة
- احرص على عدم تجاوز حدود الخادم

## الدعم

### المساعدة
- تحقق من سجلات النظام
- تأكد من صحة الإعدادات
- راجع المتطلبات

### التطوير
- أضف مقاييس جديدة
- حسّن الواجهة
- أضف ميزات جديدة

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.

---

**ملاحظة**: هذا النظام مخصص للاستخدام الداخلي. تأكد من تطبيق إجراءات الأمان المناسبة قبل النشر في بيئة الإنتاج.






