# أداة مشاهدة تذاكر Zoho - المميزات

## 📋 نظرة عامة
أداة Python لعرض بيانات جدول `zoho_tickets_cache` من قاعدة بيانات PostgreSQL.

## ✨ المميزات الرئيسية

### 1. 📊 عرض عدد الصفوف الفعلي
- يعرض العدد الكامل للصفوف من قاعدة البيانات
- تنسيق واضح مع فواصل الأرقام (مثل: 6,551 صف)
- يتم التحديث تلقائياً عند تحميل البيانات

### 2. 🔄 حالات التحميل الواضحة
- **🔄 جاري التحميل**: أثناء تحميل البيانات
- **✅ تم التحميل بنجاح**: عند اكتمال التحميل
- **❌ فشل الاتصال**: في حالة الخطأ
- **التقدم في الوقت الفعلي**: (100/500) أثناء التحميل

### 3. 📈 زر الإحصائيات التفصيلية
عرض إحصائيات شاملة:
- ✅ إجمالي التذاكر
- 🔴 التذاكر المغلقة
- 🟢 التذاكر المفتوحة
- 🟡 قيد العمل
- 🚫 المغلقة يدوياً (غير Auto Close)

### 4. 📁 عرض Raw Data
- عمود `Raw Data` مدرج الآن
- عرض JSON مع تنسيق واضح
- النقر المزدوج على أي صف لفتح نافذة البيانات الكاملة
- عرض جميع البيانات بشكل منسق

### 5. 🎨 واجهة مستخدم محسنة
- تصميم عصري مع ألوان واضحة
- رسوم إيموجية للتوضيح
- حجم خط مناسب للقراءة
- إمكانية التمرير الأفقية والعمودية

## 🚀 الاستخدام

### التثبيت
```bash
pip install -r requirements.txt
```

### التشغيل
```bash
python zoho_tickets_viewer.py
```

## 🎯 الأعمدة المعروضة

1. **ID** - المعرف الفريد
2. **Zoho Ticket ID** - معرف التذكرة في Zoho
3. **Ticket Number** - رقم التذكرة
4. **User ID** - معرف المستخدم
5. **Closed By** - أغلقت بواسطة
6. **Subject** - عنوان التذكرة
7. **Status** - الحالة
8. **Department ID** - معرف القسم
9. **Created At Zoho** - تاريخ الإنشاء
10. **Closed At Zoho** - تاريخ الإغلاق
11. **Response Time** - وقت الاستجابة (بالدقائق)
12. **Thread Count** - عدد المشاركات
13. **Created At** - تاريخ الإنشاء في النظام
14. **Raw Data** - البيانات الخام (JSON)

## 🛠️ الميزات التقنية

- **معالجة JSON**: تحويل JSON data بشكل صحيح
- **تقصير النصوص الطويلة**: عرض أول 200-300 حرف مع "..."
- **أمان البيانات**: معالجة أخطاء الاتصال
- **أداء محسّن**: تحديثات كل 100 صف
- **تنسيق أرقام**: فواصل الأرقام للقراءة السهلة

## 📝 الوظائف المتاحة

### 🔄 تحديث
- إعادة تحميل جميع البيانات من قاعدة البيانات
- تحديث العدد والواجهة

### 📈 إحصائيات
- عرض إحصائيات مفصلة عن التذاكر
- تصنيف حسب الحالة
- أرقام مع فواصل للقراءة

### 📋 النقر المزدوج
- النقر المزدوج على أي صف
- عرض جميع البيانات الكاملة في نافذة منبثقة
- قابل للنسخ والتمرير

## ⚙️ إعدادات قاعدة البيانات

يمكن تعديل إعدادات الاتصال في الكود:
```python
self.db_config = {
    'host': '127.0.0.1',
    'port': 5432,
    'database': 'CRM_ALL',
    'user': 'postgres',
    'password': ''
}
```

## 🎓 الأمثلة

### مثال 1: عرض البيانات
```
python zoho_tickets_viewer.py
```
عند التشغيل، ستحصل على نافذة تعرض:
- عدد الصفوف: 6,551 صف ✅
- جدول كامل بجميع البيانات
- أزرار التحديث والإحصائيات

### مثال 2: استخدام الإحصائيات
1. اضغط على زر "📈 إحصائيات"
2. ستظهر نافذة منبثقة مع:
   ```
   📊 إحصائيات تذاكر Zoho
   ━━━━━━━━━━━━━━━━━━━━━━━
   ✅ إجمالي التذاكر: 6,551
   🔴 التذاكر المغلقة: 4,230
   ...
   ```

### مثال 3: عرض Raw Data
1. انقر نقرة مزدوجة على أي صف
2. ستفتح نافذة تعرض:
   - جميع البيانات الكاملة
   - بيانات JSON مع تنسيق
   - قابل للتمرير والإغلاق

## 🔧 استكشاف الأخطاء

### خطأ الاتصال بقاعدة البيانات
- تأكد من أن PostgreSQL يعمل
- تحقق من صحة بيانات الاتصال
- تأكد من وجود قاعدة البيانات `CRM_ALL`
- تأكد من وجود جدول `zoho_tickets_cache`

### لا تظهر البيانات
- تأكد من وجود بيانات في الجدول
- اضغط على "🔄 تحديث"
- تحقق من رسائل الخطأ في الكونسول

## 📞 الدعم

للأسئلة أو المشاكل، راجع الكود المصدري في `zoho_tickets_viewer.py`

