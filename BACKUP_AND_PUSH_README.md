# سكريبتات النسخ الاحتياطي والرفع على Git

هذا المجلد يحتوي على سكريبتات لأخذ نسخة احتياطية من قاعدة البيانات ورفع المشروع كاملاً على Git متضمن قاعدة البيانات.

## الملفات المتوفرة

1. **`backup_and_push.sh`** - سكريبت Bash لنظام Linux/Mac/Git Bash
2. **`backup_and_push.bat`** - سكريبت Batch لنظام Windows
3. **`backup_and_push.php`** - سكريبت PHP (يعمل على جميع الأنظمة)

## المتطلبات

### للـ Bash Script (Linux/Mac/Git Bash):
- `mysqldump` أو `pg_dump` (حسب نوع قاعدة البيانات)
- `gzip` لضغط الملفات
- `git` مثبت ومضبوط

### للـ Batch Script (Windows):
- `mysqldump` أو `pg_dump` في PATH
- `git` مثبت ومضبوط
- PowerShell (مدمج في Windows)

### للـ PHP Script:
- PHP 7.4 أو أحدث
- `mysqldump` أو `pg_dump` في PATH
- `git` مثبت ومضبوط
- إمكانية تنفيذ الأوامر من PHP

## الاستخدام

### Linux/Mac/Git Bash:
```bash
./backup_and_push.sh
```

### Windows (Command Prompt):
```cmd
backup_and_push.bat
```

### Windows (PowerShell):
```powershell
.\backup_and_push.bat
```

### PHP (جميع الأنظمة):
```bash
php backup_and_push.php
```

## ما يقوم به السكريبت

1. ✅ قراءة إعدادات قاعدة البيانات من ملف `.env`
2. ✅ أخذ نسخة احتياطية كاملة من قاعدة البيانات
3. ✅ ضغط الملف الاحتياطي لتوفير المساحة
4. ✅ حفظ النسخة الاحتياطية في مجلد `database_backups/`
5. ✅ إضافة جميع الملفات (بما في ذلك قاعدة البيانات) إلى Git
6. ✅ عمل commit مع رسالة تحتوي على التاريخ والوقت
7. ✅ رفع التغييرات إلى Git (push)

## الملفات الاحتياطية

- يتم حفظ النسخ الاحتياطية في مجلد `database_backups/`
- اسم الملف: `{database_name}_backup_{timestamp}.sql.gz`
- مثال: `crm_backup_20241119_133045.sql.gz`

## استعادة النسخة الاحتياطية

### MySQL/MariaDB:
```bash
# فك الضغط
gunzip database_backups/crm_backup_20241119_133045.sql.gz

# استعادة
mysql -u username -p database_name < database_backups/crm_backup_20241119_133045.sql
```

### PostgreSQL:
```bash
# فك الضغط
gunzip database_backups/crm_backup_20241119_133045.sql.gz

# استعادة
psql -U username -d database_name -f database_backups/crm_backup_20241119_133045.sql
```

## ملاحظات مهمة

⚠️ **تحذير**: السكريبتات تضيف قاعدة البيانات كاملة إلى Git. تأكد من:
- أن المستودع خاص أو أن لديك صلاحيات مناسبة
- أن قاعدة البيانات لا تحتوي على معلومات حساسة
- أن حجم قاعدة البيانات معقول للرفع على Git

💡 **نصيحة**: يمكنك استخدام `.gitignore` لاستثناء ملفات معينة من الرفع.

## استكشاف الأخطاء

### خطأ: "mysqldump: command not found"
- تأكد من تثبيت MySQL Client
- أضف مسار MySQL إلى PATH

### خطأ: "pg_dump: command not found"
- تأكد من تثبيت PostgreSQL Client
- أضف مسار PostgreSQL إلى PATH

### خطأ: "Permission denied"
- تأكد من صلاحيات الكتابة في المجلد
- تأكد من صلاحيات Git

### خطأ: "Git push failed"
- تأكد من إعداد Git remote
- تأكد من صلاحيات الرفع على المستودع
- تحقق من اتصال الإنترنت

## التخصيص

يمكنك تعديل السكريبتات حسب احتياجاتك:
- تغيير اسم مجلد النسخ الاحتياطي
- إضافة خيارات إضافية لـ mysqldump/pg_dump
- تغيير طريقة الضغط
- إضافة إشعارات أو إرسال بريد إلكتروني

## الدعم

إذا واجهت أي مشاكل، تأكد من:
1. قراءة رسائل الخطأ بعناية
2. التحقق من إعدادات قاعدة البيانات في `.env`
3. التحقق من إعدادات Git
4. مراجعة سجلات الأخطاء

