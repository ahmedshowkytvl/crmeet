# الهيكل التنظيمي الديناميكي - Dynamic Organizational Chart

## نظرة عامة

تم إنشاء صفحة منفصلة للهيكل التنظيمي الديناميكي التي تستخدم نفس مرجعية الـ Map ولكن تكون متصلة بالنظام الحالي وتعرض البيانات الحقيقية من قاعدة البيانات.

### 🎯 **المميزات الرئيسية:**

#### ✅ **ديناميكية كاملة:**
- بيانات حقيقية من قاعدة البيانات
- تحديث تلقائي عند تغيير البيانات
- ربط مع النظام الحالي (Users, Departments, Roles)

#### ✅ **نفس مرجعية Map:**
- نفس التصميم والهيكل
- نفس الخطوط والاتصالات
- نفس التفاعل والوظائف

#### ✅ **بيانات شاملة:**
- معلومات الموظفين الكاملة
- صور شخصية من النظام
- معلومات الأقسام والأدوار
- علاقات الإدارة والتبعية

## الملفات المنشأة

### 1. الصفحة الرئيسية
- `resources/views/dynamic-org-chart.blade.php`
- صفحة منفصلة Full Screen مع نفس تصميم Map

### 2. الـ Controller
- `app/Http/Controllers/DynamicOrgChartController.php`
- معالجة البيانات الديناميكية والتحويل

### 3. المسارات
تم إضافة المسارات في `routes/web.php`:
- `GET /dynamic-org-chart` - عرض الصفحة
- `GET /api/organizational-chart` - جلب بيانات الموظفين
- `GET /api/org-statistics` - إحصائيات النظام
- `GET /api/department-hierarchy` - هيكل الأقسام
- `POST /api/org-export` - تصدير البيانات

## البيانات المدعومة

### 👥 **الموظفون:**
- الاسم (عربي/إنجليزي)
- المنصب/المسمى الوظيفي
- الصورة الشخصية
- معلومات الاتصال
- تاريخ التوظيف
- القسم التابع له
- الدور في النظام
- المدير المباشر
- المرؤوسين

### 🏢 **الأقسام:**
- اسم القسم (عربي/إنجليزي)
- كود القسم
- الوصف
- المدير
- المدير الأول
- قادة الفرق
- عدد الموظفين

### 📊 **الإحصائيات:**
- إجمالي الموظفين
- إجمالي الأقسام
- عدد المديرين
- عدد الموظفين العاديين
- توزيع الموظفين حسب القسم
- توزيع الموظفين حسب الدور

## كيفية الاستخدام

### الوصول للصفحة:
```
/dynamic-org-chart
```

### من الداشبورد:
- زر "الهيكل التنظيمي (ديناميكي)" في Quick Actions
- يفتح في تبويب جديد

### API Endpoints:
```javascript
// جلب بيانات الموظفين
GET /api/organizational-chart

// جلب الإحصائيات
GET /api/org-statistics

// جلب هيكل الأقسام
GET /api/department-hierarchy

// تصدير البيانات
POST /api/org-export
```

## البنية التقنية

### 🔧 **Backend:**
- Laravel Controller مع Eloquent ORM
- علاقات متقدمة بين Models
- تحويل البيانات للشكل المطلوب
- معالجة الأخطاء والاستثناءات

### 🎨 **Frontend:**
- نفس تصميم Map مع تحسينات
- JavaScript محسن للبيانات الديناميكية
- معالجة حالات التحميل والأخطاء
- تفاعل سلس مع البيانات الحقيقية

### 📡 **API:**
- RESTful API endpoints
- JSON responses
- معالجة الأخطاء
- إحصائيات مفصلة

## الميزات المتقدمة

### ✨ **التفاعل:**
- عرض تفاصيل الموظف
- قائمة خيارات لكل موظف
- عرض المهام
- تعديل الموظف
- حذف الموظف

### 📱 **الاستجابة:**
- متوافق مع جميع الشاشات
- تحسين للهواتف الذكية
- دعم اللمس

### 🔄 **التحديث:**
- بيانات ديناميكية
- تحديث تلقائي
- معالجة التغييرات

## الفروق عن النسخة الثابتة

| الميزة | النسخة الثابتة | النسخة الديناميكية |
|--------|----------------|-------------------|
| البيانات | ثابتة من map.json | ديناميكية من قاعدة البيانات |
| الصور | ثابتة | من النظام الحالي |
| التحديث | يدوي | تلقائي |
| الإحصائيات | ثابتة | حقيقية |
| الأقسام | ثابتة | من النظام |
| الأدوار | ثابتة | من النظام |

## المتطلبات

- Laravel Framework
- قاعدة بيانات مع جداول Users, Departments, Roles
- علاقات صحيحة بين الجداول
- صور الموظفين في storage

## الدعم

الصفحة متوافقة مع:
- جميع المتصفحات الحديثة
- الأجهزة المحمولة
- وضع الطباعة
- وضع ملء الشاشة

## التطوير المستقبلي

يمكن إضافة:
- تحرير البيانات مباشرة
- إضافة/حذف الموظفين
- تصدير الهيكل كصورة
- مشاركة الهيكل
- تزامن مع أنظمة خارجية
- إشعارات التغييرات
- نسخ احتياطية

---

تم إنشاء هذا الهيكل التنظيمي الديناميكي ليعمل مع النظام الحالي ويوفر تجربة تفاعلية مع البيانات الحقيقية.
