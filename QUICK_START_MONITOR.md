# دليل البدء السريع - مراقب النظام

## 🚀 البدء في 3 خطوات

### 1. تشغيل النظام
```bash
# على Windows
start-system-monitor.bat

# على Linux/Mac
./start-system-monitor.sh
```

### 2. فتح المراقب
افتح المتصفح واذهب إلى:
```
http://localhost:8000/system-monitor
```

### 3. مشاركة الرابط
أرسل هذا الرابط لأي شخص على نفس الشبكة:
```
http://YOUR_IP:8000/system-monitor
```

## 📱 الوصول من الشبكة

### معرفة عنوان IP
```bash
# Windows
ipconfig

# Linux/Mac
ifconfig
```

### مثال
إذا كان عنوان IP هو `192.168.1.100`:
```
http://192.168.1.100:8000/system-monitor
```

## 🔧 إعدادات سريعة

### تغيير المنفذ
```bash
# في start-system-monitor.bat
php artisan serve --host=0.0.0.0 --port=9000

# في websocket-server.js
const server = new SystemMonitorWebSocketServer(9080);
```

### إضافة حماية
```php
// في routes/web.php
Route::middleware(['auth'])->group(function () {
    Route::get('/system-monitor', [SystemMonitorController::class, 'index']);
});
```

## ⚡ ميزات سريعة

- **تحديث تلقائي**: كل 5 ثوان
- **رسوم بيانية**: استخدام الذاكرة والأداء
- **تنبيهات**: عند تجاوز الحدود
- **مستخدمين نشطين**: من هم متصلون حالياً
- **معلومات النظام**: PHP، قاعدة البيانات، الخادم

## 🆘 حل المشاكل

### لا يعمل WebSocket
```bash
npm install ws
node websocket-server.js
```

### لا يعمل Laravel
```bash
php artisan serve --host=0.0.0.0 --port=8000
```

### مشاكل الشبكة
- تأكد من فتح المنافذ 8000 و 8080
- تحقق من إعدادات الجدار الناري
- تأكد من أن الجميع على نفس الشبكة

## 📞 المساعدة

- راجع `SYSTEM_MONITOR_README.md` للتفاصيل الكاملة
- تحقق من سجلات النظام في `storage/logs/laravel.log`
- تأكد من تثبيت جميع المتطلبات

---

**نصيحة**: احفظ هذا الملف للرجوع إليه لاحقاً! 🎯






