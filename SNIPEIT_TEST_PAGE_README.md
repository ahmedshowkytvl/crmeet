# صفحة اختبار Snipe-IT API

تم إنشاء صفحة اختبار شاملة لـ Snipe-IT API مع JavaScript للتفاعل المباشر.

## 🚀 المميزات

### ✅ **تم إنشاؤها:**
- ✅ صفحة اختبار شاملة: `http://127.0.0.1:8000/zoho/snipe-it`
- ✅ اختبار الاتصال مع Snipe-IT API
- ✅ جلب بيانات المستخدم الحالي
- ✅ اختبار جلب الأصول (تجريبي)
- ✅ اختبار جلب الفئات (تجريبي)
- ✅ اختبار جلب المستخدمين (تجريبي)
- ✅ اختبار شامل لجميع الوظائف
- ✅ واجهة مستخدم تفاعلية مع JavaScript
- ✅ عرض النتائج مباشرة على الصفحة

## 📁 الملفات المُنشأة:

### 1. **Controller:**
- `app/Http/Controllers/SnipeItController.php` - تم إضافة method `testPage()` و `getUser()`

### 2. **Routes:**
- `routes/web.php` - تم إضافة route `/zoho/snipe-it`

### 3. **Views:**
- `resources/views/snipe-it/test-page.blade.php` - صفحة الاختبار الكاملة

### 4. **Service:**
- `app/Services/SnipeItService.php` - تم إضافة method `getCurrentUser()`

### 5. **Config:**
- `config/snipeit.php` - ملف إعدادات Snipe-IT
- `.env.example` - تم إضافة متغيرات البيئة

## 🎯 كيفية الاستخدام:

### **الخطوة 1: الوصول للصفحة**
```
http://127.0.0.1:8000/zoho/snipe-it
```

### **الخطوة 2: اختبار الاتصال**
- اضغط على زر "اختبار الاتصال"
- ستظهر النتيجة مباشرة على الصفحة

### **الخطوة 3: جلب بيانات المستخدم**
- اضغط على زر "جلب بيانات المستخدم"
- ستظهر بيانات المستخدم الحالي من Snipe-IT

### **الخطوة 4: اختبار شامل**
- اضغط على زر "تشغيل جميع الاختبارات"
- سيتم تشغيل جميع الاختبارات تلقائياً

## 🔧 الإعدادات:

### **في ملف .env:**
```env
SNIPEIT_API_URL=http://127.0.0.1:8080
SNIPEIT_API_TOKEN=YOUR_API_TOKEN_HERE
```

### **في ملف config/snipeit.php:**
```php
'api_url' => env('SNIPEIT_API_URL', 'http://127.0.0.1:8080'),
'api_token' => env('SNIPEIT_API_TOKEN'),
```

## 📋 الوظائف المتاحة:

### **1. اختبار الاتصال:**
```javascript
fetch('/api/snipe-it/test-connection', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').getAttribute('content')
    }
})
```

### **2. جلب بيانات المستخدم:**
```javascript
fetch('/api/snipe-it/get-user', {
    method: 'GET',
    headers: {
        'Content-Type': 'application/json',
        'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').getAttribute('content')
    }
})
```

## 🎨 واجهة المستخدم:

### **الأزرار المتاحة:**
- 🔌 **اختبار الاتصال** - اختبار الاتصال مع Snipe-IT
- 👤 **جلب بيانات المستخدم** - عرض بيانات المستخدم الحالي
- 📦 **جلب الأصول** - قائمة الأصول (تجريبي)
- 📂 **جلب الفئات** - قائمة الفئات (تجريبي)
- 👥 **جلب المستخدمين** - قائمة المستخدمين (تجريبي)
- 🚀 **تشغيل جميع الاختبارات** - اختبار شامل

### **عرض النتائج:**
- ✅ نجح الاختبار - خلفية خضراء
- ❌ فشل الاختبار - خلفية حمراء
- ⚠️ تحذير - خلفية صفراء
- ℹ️ معلومات - خلفية زرقاء

## 🔄 JavaScript التفاعلي:

### **المميزات:**
- تحديث البيانات مباشرة على الصفحة
- عدم إعادة تحميل الصفحة
- عرض رسائل التحميل
- معالجة الأخطاء
- عرض النتائج بشكل منظم

### **مثال على الاستخدام:**
```javascript
document.getElementById('testUserBtn').addEventListener('click', function() {
    // تعطيل الزر أثناء التحميل
    this.disabled = true;
    this.innerHTML = '<i class="fas fa-spinner fa-spin"></i> جاري الجلب...';
    
    // إرسال الطلب
    fetch('/api/snipe-it/get-user')
        .then(response => response.json())
        .then(data => {
            // عرض البيانات
            document.getElementById('uid').textContent = data.data.id;
            document.getElementById('ufirstname').textContent = data.data.first_name;
            // ... باقي البيانات
        })
        .finally(() => {
            // إعادة تفعيل الزر
            this.disabled = false;
            this.innerHTML = '<i class="fas fa-user"></i> جلب بيانات المستخدم';
        });
});
```

## 🚨 ملاحظات مهمة:

1. **Snipe-IT يجب أن يكون مثبت ومتاح**
2. **API Token يجب أن يكون صحيح**
3. **المنفذ الافتراضي هو 8080**
4. **جميع الطلبات محمية بـ CSRF Token**

## 🔧 استكشاف الأخطاء:

### **إذا فشل الاتصال:**
1. تأكد من أن Snipe-IT يعمل
2. تحقق من صحة API Token
3. تأكد من المنفذ الصحيح
4. راجع logs Laravel

### **إذا فشل جلب البيانات:**
1. تحقق من صلاحيات API Token
2. تأكد من وجود البيانات في Snipe-IT
3. راجع إعدادات Snipe-IT

## 🎉 النتيجة النهائية:

الآن لديك صفحة اختبار شاملة لـ Snipe-IT API تعمل بالضبط كما طلبت:

- ✅ زر "اختبار الاتصال" يعمل مع JavaScript
- ✅ البيانات تظهر مباشرة على الصفحة
- ✅ لا حاجة لإعادة تحميل الصفحة
- ✅ واجهة مستخدم جميلة ومنظمة
- ✅ معالجة شاملة للأخطاء
- ✅ عرض النتائج بشكل واضح

**الرابط:** `http://127.0.0.1:8000/zoho/snipe-it`
