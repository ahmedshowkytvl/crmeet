# نظام الدردشة الداخلية - Internal Chat System

## نظرة عامة

نظام الدردشة الداخلية هو نظام اتصال متكامل يسمح للموظفين بالتواصل مع بعضهم البعض داخل النظام. يدعم النظام الرسائل النصية، رفع الملفات، مشاركة جهات الاتصال، والإشعارات الفورية.

## المميزات الرئيسية

### 🔥 المميزات الأساسية
- **دردشة فردية**: تواصل مباشر بين موظفين
- **رسائل نصية**: إرسال واستقبال الرسائل النصية
- **رفع الملفات**: دعم رفع الصور والملفات (PDF، Word، Excel، إلخ)
- **مشاركة جهات الاتصال**: إرسال معلومات الموظفين كجهات اتصال
- **إشعارات فورية**: إشعارات في الوقت الفعلي للرسائل الجديدة
- **دعم RTL**: واجهة باللغة العربية مع دعم كامل للكتابة من اليمين لليسار

### ✨ المميزات المتقدمة
- **حالات الرسائل**: مرسل، تم التسليم، مقروء
- **أرشفة الدردشات**: إمكانية أرشفة الدردشات القديمة
- **كتم الصوت**: إمكانية كتم إشعارات دردشة معينة
- **البحث**: البحث في الدردشات والرسائل
- **إحصائيات**: إحصائيات مفصلة للدردشات والرسائل
- **أمان متقدم**: حماية كاملة للبيانات والرسائل

## البنية التقنية

### قاعدة البيانات

#### جدول `chat_rooms`
```sql
- id (Primary Key)
- name (اسم الدردشة)
- description (وصف الدردشة)
- type (نوع الدردشة: private/group)
- avatar (صورة الدردشة)
- is_active (حالة الدردشة)
- last_message_at (آخر رسالة)
- created_at, updated_at
```

#### جدول `chat_messages`
```sql
- id (Primary Key)
- chat_room_id (Foreign Key)
- sender_id (Foreign Key)
- receiver_id (Foreign Key)
- message_type (نوع الرسالة: text/image/file/contact/system)
- content (محتوى الرسالة)
- attachment_url (رابط المرفق)
- attachment_name (اسم المرفق)
- attachment_size (حجم المرفق)
- attachment_type (نوع المرفق)
- metadata (بيانات إضافية JSON)
- status (حالة الرسالة: sent/delivered/read)
- read_at (وقت القراءة)
- is_edited (تم التعديل)
- edited_at (وقت التعديل)
- is_deleted (تم الحذف)
- deleted_at (وقت الحذف)
- created_at, updated_at
```

#### جدول `chat_participants`
```sql
- id (Primary Key)
- chat_room_id (Foreign Key)
- user_id (Foreign Key)
- role (دور المشارك: admin/member)
- joined_at (وقت الانضمام)
- last_read_at (آخر قراءة)
- is_muted (كتم الصوت)
- is_archived (أرشفة)
- created_at, updated_at
```

### النماذج (Models)

#### `ChatRoom`
- إدارة غرف الدردشة
- العلاقات مع الرسائل والمشاركين
- دوال مساعدة للحصول على المعلومات

#### `ChatMessage`
- إدارة الرسائل
- دعم أنواع مختلفة من الرسائل
- دوال لتحديث حالة الرسائل

#### `ChatParticipant`
- إدارة المشاركين في الدردشة
- تتبع حالة القراءة والإعدادات

### المتحكمات (Controllers)

#### `ChatController`
- إدارة الدردشات
- بدء دردشات جديدة
- البحث في الدردشات
- إدارة إعدادات الدردشة

#### `ChatMessageController`
- إرسال الرسائل
- إدارة الرسائل
- البحث في الرسائل
- تحديث حالات الرسائل

#### `ChatFileController`
- رفع الملفات
- تحميل الملفات
- إدارة المرفقات

## التثبيت والإعداد

### 1. تشغيل الـ Migrations
```bash
php artisan migrate
```

### 2. إعداد الصلاحيات
تأكد من أن المستخدمين لديهم صلاحية الوصول لنظام الدردشة.

### 3. إعداد الإشعارات
```bash
# تشغيل queue worker للإشعارات
php artisan queue:work
```

### 4. إعداد التخزين
تأكد من أن مجلد `storage/app/public` قابل للكتابة:
```bash
php artisan storage:link
```

## الاستخدام

### بدء دردشة جديدة
1. انتقل إلى صفحة الدردشة
2. انقر على زر "بدء دردشة جديدة"
3. ابحث عن الموظف المطلوب
4. انقر على اسم الموظف لبدء الدردشة

### إرسال رسالة نصية
1. افتح الدردشة المطلوبة
2. اكتب رسالتك في حقل النص
3. انقر على زر الإرسال أو اضغط Enter

### رفع ملف
1. انقر على زر المرفق (📎)
2. اختر الملف المطلوب
3. سيتم رفع الملف تلقائياً وإرساله

### مشاركة جهة اتصال
1. انقر على زر مشاركة جهة الاتصال (👤+)
2. ابحث عن الموظف المطلوب
3. انقر على اسم الموظف لمشاركته

### البحث في الرسائل
1. انقر على زر البحث في الدردشة
2. اكتب الكلمة المطلوبة
3. ستظهر النتائج المطابقة

## الإعدادات

### إعدادات الإشعارات
يمكن للمستخدمين تخصيص إعدادات الإشعارات من صفحة الإعدادات:
- الإشعارات الصوتية
- إشعارات سطح المكتب
- إشعارات البريد الإلكتروني

### إعدادات الدردشة
- كتم صوت دردشة معينة
- أرشفة الدردشات القديمة
- إظهار حالة الاتصال

## الأمان

### حماية البيانات
- جميع الرسائل مشفرة في قاعدة البيانات
- الملفات محفوظة في مجلد آمن
- التحقق من الصلاحيات قبل كل عملية

### حماية الخصوصية
- المستخدمون يمكنهم الوصول فقط لدردشاتهم
- لا يمكن للمستخدمين رؤية دردشات الآخرين
- إمكانية حذف الرسائل نهائياً

## الأداء

### تحسين الاستعلامات
- فهرسة قاعدة البيانات محسنة
- استعلامات محسنة للرسائل والدردشات
- تحميل كسول للبيانات

### تحسين التخزين
- ضغط الصور تلقائياً
- حذف الملفات المؤقتة
- تنظيف قاعدة البيانات الدوري

## الاختبار

### اختبارات Playwright
```bash
# تشغيل اختبارات الدردشة
npx playwright test tests/chat_system_test.js
```

### اختبارات الوحدة
```bash
# تشغيل اختبارات Laravel
php artisan test --filter=Chat
```

## استكشاف الأخطاء

### مشاكل شائعة

#### لا تظهر الدردشات
- تأكد من تسجيل الدخول
- تحقق من صلاحيات المستخدم
- تأكد من تشغيل الـ migrations

#### لا يتم إرسال الرسائل
- تحقق من اتصال الإنترنت
- تأكد من صحة البيانات المدخلة
- تحقق من سجلات الأخطاء

#### لا تعمل الإشعارات
- تأكد من تشغيل queue worker
- تحقق من إعدادات الإشعارات
- تأكد من صحة إعدادات البريد الإلكتروني

### سجلات الأخطاء
```bash
# عرض سجلات Laravel
tail -f storage/logs/laravel.log

# عرض سجلات queue
tail -f storage/logs/queue.log
```

## التطوير المستقبلي

### ميزات مخططة
- **دردشات جماعية**: دعم الدردشات متعددة المشاركين
- **مكالمات صوتية**: إضافة مكالمات صوتية
- **مكالمات فيديو**: إضافة مكالمات فيديو
- **مشاركة الشاشة**: إمكانية مشاركة الشاشة
- **ردود سريعة**: قوالب رسائل جاهزة
- **ردود تلقائية**: ردود تلقائية عند عدم التوفر

### تحسينات الأداء
- **WebSocket**: دعم الاتصال المباشر
- **Push Notifications**: إشعارات فورية
- **تحسين البحث**: بحث متقدم في الرسائل
- **ضغط البيانات**: ضغط أفضل للرسائل

## الدعم والمساعدة

### التوثيق
- [دليل المطور](DEVELOPER_GUIDE.md)
- [دليل API](API_DOCUMENTATION.md)
- [دليل التخصيص](CUSTOMIZATION_GUIDE.md)

### التواصل
- البريد الإلكتروني: support@company.com
- الهاتف: +966-XX-XXX-XXXX
- نظام التذاكر الداخلي

## الترخيص

هذا النظام مرخص تحت رخصة MIT. راجع ملف LICENSE للتفاصيل.

---

**ملاحظة**: هذا النظام مصمم للاستخدام الداخلي في الشركة فقط. لا يُسمح بالاستخدام الخارجي أو التوزيع بدون إذن صريح.
