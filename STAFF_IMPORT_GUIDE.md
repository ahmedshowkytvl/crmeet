# دليل استيراد بيانات الموظفين من Excel

## 🎯 الهدف
استيراد بيانات الموظفين من ملف Excel إلى النظام مع ضمان التعامل الصحيح مع النص العربي.

## 📁 الملفات المتاحة

### 1. `test_excel_import.php`
**الغرض**: اختبار قراءة ملف Excel وفحص الترميز
```bash
php test_excel_import.php
```

### 2. `create_sample_excel.php`
**الغرض**: إنشاء ملف Excel نموذجي للاختبار
```bash
php create_sample_excel.php
```

### 3. `import_staff_excel.php`
**الغرض**: استيراد بيانات الموظفين من ملف Excel
```bash
php import_staff_excel.php "staff list 2025.xlsx"
```

### 4. `import_real_staff.php`
**الغرض**: استيراد البيانات الحقيقية مع معالجة متقدمة
```bash
php import_real_staff.php
```

## 🔧 المتطلبات

### 1. تثبيت المكتبات المطلوبة
```bash
composer install
```

### 2. إعداد قاعدة البيانات
تأكد من أن قاعدة البيانات تعمل وأن الجداول التالية موجودة:
- `users`
- `departments`
- `roles`

## 📋 الحقول المدعومة

### الحقول الأساسية (مطلوبة)
- `name` - الاسم
- `email` - البريد الإلكتروني

### الحقول الإضافية (اختيارية)
- `name_ar` - الاسم بالعربية
- `work_email` - البريد الوظيفي
- `phone_work` - هاتف العمل
- `phone_personal` - الهاتف الشخصي
- `job_title` - المسمى الوظيفي
- `position` - المنصب
- `position_ar` - المنصب بالعربية
- `department` - القسم
- `role` - الدور
- `manager` - المدير
- `address` - العنوان
- `address_ar` - العنوان بالعربية
- `birth_date` - تاريخ الميلاد
- `bio` - نبذة شخصية
- `notes` - ملاحظات
- `nationality` - الجنسية
- `city` - المدينة
- `country` - البلد

## 🌍 دعم النص العربي

### الترميز المدعوم
- UTF-8 (مفضل)
- Windows-1256
- ISO-8859-6

### معالجة النص العربي
- تحويل تلقائي للترميز
- تنظيف الأحرف غير المرغوب فيها
- إزالة المسافات الزائدة
- دعم التواريخ العربية

## 📊 خطوات الاستيراد

### 1. اختبار الملف
```bash
php test_excel_import.php
```

### 2. إنشاء ملف نموذجي (اختياري)
```bash
php create_sample_excel.php
```

### 3. استيراد البيانات
```bash
php import_real_staff.php
```

## ⚠️ ملاحظات مهمة

### 1. تنسيق ملف Excel
- يجب أن يكون الملف بصيغة `.xlsx` أو `.xls`
- يجب أن تكون العناوين في الصف الأول
- يجب أن تكون البيانات في الصفوف التالية

### 2. معالجة الأخطاء
- يتم تخطي الصفوف الفارغة
- يتم تخطي المستخدمين الموجودين مسبقاً
- يتم تسجيل الأخطاء في قائمة منفصلة

### 3. البيانات المرجعية
- يجب أن تكون الأقسام موجودة في جدول `departments`
- يجب أن تكون الأدوار موجودة في جدول `roles`
- يجب أن يكون المديرون موجودين في جدول `users`

## 🔍 استكشاف الأخطاء

### مشاكل الترميز
```bash
# فحص ترميز الملف
php test_excel_import.php
```

### مشاكل قاعدة البيانات
```bash
# فحص الاتصال
php artisan tinker
```

### مشاكل الصلاحيات
```bash
# فحص الصلاحيات
php artisan route:list
```

## 📈 النتائج المتوقعة

### عند النجاح
```
✅ نجح: 25
⚠️  تم تخطيه: 3
❌ فشل: 0
📝 إجمالي: 28
```

### عند وجود أخطاء
```
✅ نجح: 20
⚠️  تم تخطيه: 5
❌ فشل: 3
📝 إجمالي: 28

❌ الأخطاء:
   - الصف 5: الحقل المطلوب 'email' فارغ
   - الصف 12: قسم غير موجود: قسم غير صحيح
   - الصف 18: خطأ في إنشاء المستخدم: البريد الإلكتروني مكرر
```

## 🚀 نصائح للاستخدام الأمثل

### 1. إعداد ملف Excel
- استخدم العناوين باللغة العربية أو الإنجليزية
- تأكد من صحة تنسيق التواريخ
- تحقق من صحة أرقام الهواتف

### 2. إعداد قاعدة البيانات
- تأكد من وجود الأقسام المطلوبة
- تأكد من وجود الأدوار المطلوبة
- تأكد من وجود المديرين المطلوبين

### 3. مراقبة النتائج
- راجع قائمة الأخطاء بعناية
- تحقق من البيانات المستوردة
- اختبر تسجيل الدخول للمستخدمين الجدد

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع ملف السجلات
2. تحقق من إعدادات قاعدة البيانات
3. تأكد من صحة تنسيق ملف Excel
4. راجع قائمة الأخطاء المفصلة

---

**تم إنشاء هذا الدليل لضمان استيراد ناجح وآمن لبيانات الموظفين** 🎉
